services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend-prod
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
